# Test dependencies for the CPU-only test suite (the GPU-free helper functions).
# Lower-bound ranges so the suite resolves across supported Python versions.
biopython>=1.83
numpy>=1.26,<3
scipy>=1.11
pytest>=8
# Additionally required by the internal baseline/ tests (scheduler, scorers,
# add_ipsae) -- not needed for the upstream helper tests.
pandas>=2
